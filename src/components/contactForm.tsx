'use client'

import styles from '../styles/contact.module.scss'

export default function ContactC() {

  return (
    <main className={styles.contactWrapper}>
      <h1 style={{color: "#fff"}}>Still in devlopment</h1>
    </main>
  )
}